require "spec_helper"

describe Selenium::Webdriver::Full::Screenshot do
  it "has a version number" do
    expect(Selenium::Webdriver::Full::Screenshot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
