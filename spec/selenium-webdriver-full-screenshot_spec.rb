require "spec_helper"

describe Selenium::Webdriver::FullScreenshot do
  it "has a version number" do
    expect(Selenium::Webdriver::FullScreenshot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
